version: '3.8'

services:
  deeplx:
    image: ghcr.io/owo-network/deeplx:latest
    restart: always
    ports:
      - "11888:11888"
    # environment:
      # - TOKEN=helloworld
      # - DL_SESSION=xxxxxx